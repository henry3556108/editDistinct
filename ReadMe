EditDistinct 裡面有 3 個 public method
init_table, get_table, evaluate 三個

evaluate(str1, str2, a, b) 應該要用丟四個參數進去
前面兩個是要被比較的字串
a, b 分別是 str1 與 str2 的字串長度
會回傳他們的 Edit Distinct

init_table(str1, str2) 
只要丟兩個要比較字串得進去就可以初始化了
要比較其他字串時要重新 init

